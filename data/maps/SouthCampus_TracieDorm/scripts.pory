mapscripts SouthCampus_TracieDorm_MapScripts { 
    MAP_SCRIPT_ON_FRAME_TABLE [
            VAR_PLAYERDORM_STATE, 3: TracieDorm_Enter
    ]
}

script TracieDorm_Enter {
    clearflag(FLAG_HIDE_TRACIE)
    lockall
    applymovement(OBJ_EVENT_ID_PLAYER, moves(walk_up*3))
    waitmovement(0)
    setspeaker(SP_NAME_TRACIE)
    msgbox(format("Hmm… Where did I put it…?\nLet's see…\pOh! Found it!\nMy {COLOR BLUE}Quest Log{COLOR DARK_GRAY}!"))
    closemessage
    playfanfare(MUS_OBTAIN_ITEM)
    msgbox(format("Tracie found her {COLOR BLUE}Quest Log{COLOR DARK_GRAY}!"))
    waitfanfare
    closemessage
    applymovement(LOCALID_TRACIE_TRACIE_DORM, moves(walk_left, walk_down))
    waitmovement(0)
    setspeaker(SP_NAME_TRACIE)
    msgbox(format("Here {PLAYER}, I actually have a second one.\pI'd like you to have it!"))
    closemessage
    playfanfare(MUS_OBTAIN_ITEM)
    msgbox(format("{PLAYER} obtained a {COLOR BLUE}Quest Log{COLOR DARK_GRAY}\nfrom Tracie!"))
    waitfanfare
    setspeaker(SP_NAME_TRACIE)
    msgbox(format("You can keep track of anything you've been asked to do!\pJust press {START_BUTTON} and select {COLOR BLUE}Quest Log {COLOR DARK_GRAY}\nto view your requests!\pYou can press {R_BUTTON} to filter them too.\pWhy don't we test it out and\nadd one for orientation!"))
    closemessage
    setflag (FLAG_SYS_QUEST_MENU_GET) 
    questmenu(QUEST_MENU_SET_ACTIVE, QUEST_ORIENTATION)
    playse(SE_DEX_SEARCH)
    msgbox(format("{COLOR BLUE}Senior Year{COLOR DARK_GRAY} has been added to your {COLOR BLUE}Quest Log{COLOR DARK_GRAY}."))
    waitse
    openquestmenu_active
    setspeaker(SP_NAME_TRACIE)
    msgbox(format("Easy! Right?\pAnyways, we can't lag too far behind Rotcart.\p{COLOR GREEN}Orientation{COLOR DARK_GRAY} is starting soon,\nafter all.\p Lead the way!\nI'll follow."))
    closemessage
    setfollowernpc(LOCALID_TRACIE_TRACIE_DORM, FNPC_ALL, TracieDorm_TracieFollowDialogue, 0)
    setflag(FLAG_HIDE_TRACIE)
    setvar(VAR_PLAYERDORM_STATE, 4)
    end
}

script TracieDorm_TracieFollowDialogue {
    faceplayer
    setspeaker(SP_NAME_TRACIE)
    msgbox(format("Hehehe… Senior year!\nLet's go! Let's go!"))
    closemessage
}

script TracieDorm_PichuDoll {
    msgbox("It's a life sized Pichu doll.")
    closemessage
}

script TracieDorm_Laptop {
    msgbox("It's Tracie's laptop.\pHer wallpaper is a picture of her\nBuneary as a baby!")
    closemessage
}
